apiVersion: v1
kind: Pod
metadata:
  name: cpu-demo
spec:
  containers:
  - name: cpu-demo-ctr
    image: vish/stress
    resources:
      limits:
        # cpu: "1000m"
        cpu: "100"
      requests:
        # cpu: "0.5"
        cpu: "100"
    args:
    - -cpus
    - "2"